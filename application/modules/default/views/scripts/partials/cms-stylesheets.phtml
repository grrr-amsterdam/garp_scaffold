<?php /** Note: keep cms.css on baseUrl, because that stylesheet is referenced from Javascript, this would result in a security error otherwise. **/ ?>
<link id="icons" rel="stylesheet" href="<?php echo $this->baseUrl($this->config()->assets->css->root . '/cms.css') . '?' . new Garp_Semver ?>">

<script>
	WYSWIWYG_CSS_URL = "<?php echo $this->assetUrl($this->config()->assets->css->root . '/cms-wysiwyg.css') ?>";
</script>
